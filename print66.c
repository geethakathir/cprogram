#include<stdio.h>
void main()
{
int num,temp,digit,sum=0;
printf("enter the number");
scanf("%d",&a);
temp=num;
while(n>0)
{
digit=num%10;
sum=sum+digit;
num/=10;
printf("Given number = %ld\n", temp);
printf("Sum of the digits %ld = %ld\n", temp, sum);
}
}
