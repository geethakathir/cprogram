#include<stdio.h>
void show voewls(int string)
int main()
{
int a,b,i;
printf("enter the total number of characters");
scanf("%d",&a);
if(i==null)
{
printf("the total characters are");
}
else
{
printf("the total characters are");
}
}
}
